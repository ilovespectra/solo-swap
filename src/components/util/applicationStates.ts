export enum ApplicationStates {
    LOADING = 0,
    LOADED_JUPYTER = 1,
    LOADED_QUOTES = 2,
    SCOOPING = 3,
    SCOOPED = 4,
  }
  
  export {}; // This ensures the file is treated as a module
  